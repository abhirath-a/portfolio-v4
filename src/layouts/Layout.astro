---
interface Props {
  title: string;
  classList?: string;
}

const { title, classList } = Astro.props;
import BaseLayout from "./BaseLayout.astro";
---

<BaseLayout title={title} classList={classList}> <slot /></BaseLayout>
